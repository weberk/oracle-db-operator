apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oracle-ords-config
spec:
  storageClassName: oci
  selector:
    matchLabels:
      failure-domain.beta.kubernetes.io/zone: "mQqW:EU-FRANKFURT-1-AD-1"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi